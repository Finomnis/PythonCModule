This folder defines an example module with a hello() function.
It can be directly imported to python.

To create custom functions, everything that needs to be modified is inside
the __src folder. Nothing outside the __src folder needs to be modified.
